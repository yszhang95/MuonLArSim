* README
Generated by ChatGPT 4o.
** How to build
#+begin_src shell
  mkdir build && cd build
  cmake -DCMAKE_EXPORT_COMPILE_COMMANDS=YES -DCMAKE_RULE_MESSAGES=OFF -S .. -B .
  # or supply the cmake with -DCMAKE_BUILD_TYPE=DEBUG
  # the source directory is specified by -S .., assuming source files are in the parent directory.
  make
#+end_src
** Usage
Without any =mac=,
: ./MuonLArSim
